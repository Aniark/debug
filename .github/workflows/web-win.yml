name: web-win
on: [workflow_dispatch]
jobs:
  build:
    runs-on: windows-latest
    env:
      CREDENTIALS: ${{ secrets.CREDENTIALS }} 
    steps:
    - uses: egor-tensin/setup-mingw@v2.2
    - uses: fawazahmed0/action-debug@main
      with:
        credentials: "${{ secrets.CREDENTIALS }}"
